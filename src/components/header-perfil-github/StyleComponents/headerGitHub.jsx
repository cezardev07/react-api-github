import styled from 'styled-components'

export const HeaderGitHub = styled.header`
    position: fixed;
    z-index: 1000;
    top: 0;
    right: 0;
    
    /* width: 100%; */
    padding: 20px;

    display: flex;
    flex-direction: column;
    align-items: flex-end;
    justify-content: center;
    gap: 10px;

    background: transparent;
`